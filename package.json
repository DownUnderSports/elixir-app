{
  "name": "DownUnderSportsElixir",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=11.15.0",
    "yarn": ">= 1.16.0"
  },
  "scripts": {
    "add-dep": "yarn --cwd assets add",
    "rm": "yarn --cwd assets remove",
    "build": "yarn --cwd assets deploy",
    "install": "yarn --cwd assets install",
    "start": "set -m; mix phx.server"
  },
  "dependencies": {
    "milligram-stylus": "^1.3.0"
  }
}
